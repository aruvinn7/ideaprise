{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo 'Starting deployment...' && python manage.py check --database default && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn arjosari.wsgi:application --bind 0.0.0.0:$PORT"
  }
}